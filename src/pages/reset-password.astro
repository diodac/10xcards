---
// src/pages/reset-password.astro
import AuthLayout from "@/layouts/AuthLayout.astro";
import ResetPasswordForm from "@/components/auth/ResetPasswordForm.tsx";

// TODO: Logic to read reset token from URL params
// const token = Astro.url.searchParams.get('token');
// if (!token) {
//   return Astro.redirect('/login'); // Or show an error
// }
---

<AuthLayout title="Resetowanie hasła">
  <h1 class="auth-card-title">Ustaw nowe hasło</h1>
  <ResetPasswordForm client:load />
  <!-- <ResetPasswordForm token={token} client:load /> -->
  <div class="mt-6 text-center">
    <p class="text-sm text-blue-100/90">
      Pamiętasz hasło lub chcesz spróbować ponownie?
      <a href="/login" class="auth-link font-medium"> Zaloguj się </a>
    </p>
  </div>
</AuthLayout>
